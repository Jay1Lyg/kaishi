//打开new_list页面
function openNewsList(tableNum,name){
	api.openWin({
	    name: 'newsList',
	    url: './news_list.html',
	    //页面间传值,在新页面中通过api.pageParam就可以获取到传过去的值
	    pageParam: {
	        type: tableNum,
	        name: name
	    }
	});
}
